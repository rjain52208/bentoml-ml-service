# Model Artifacts

This folder is a placeholder for saved BentoML model files
(e.g., `sklearn`, `xgboost`, or deep learning models).

In a real project, a training pipeline would export a model here
and the BentoML service would load it for inference.
